import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:PadrinhoMED/app/global_components/dialog_location_filter/dialog_location_filter_controller.dart';
import 'package:PadrinhoMED/app/app_module.dart';

void main() {
  initModule(AppModule());
  // DialogLocationFilterController dialoglocationfilter;
  //
  setUp(() {
    //     dialoglocationfilter = AppModule.to.get<DialogLocationFilterController>();
  });

  group('DialogLocationFilterController Test', () {
    //   test("First Test", () {
    //     expect(dialoglocationfilter, isInstanceOf<DialogLocationFilterController>());
    //   });

    //   test("Set Value", () {
    //     expect(dialoglocationfilter.value, equals(0));
    //     dialoglocationfilter.increment();
    //     expect(dialoglocationfilter.value, equals(1));
    //   });
  });
}
