import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:PadrinhoMED/app/modules/signature/signature_page.dart';

void main() {
  testWidgets('SignaturePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(SignaturePage(title: 'Signature')));
    //  final titleFinder = find.text('Signature');
    //  expect(titleFinder, findsOneWidget);
  });
}
