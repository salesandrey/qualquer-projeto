import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:PadrinhoMED/app/modules/signature/signature_controller.dart';
import 'package:PadrinhoMED/app/modules/signature/signature_module.dart';

void main() {
  initModule(SignatureModule());
  // SignatureController signature;
  //
  setUp(() {
    //     signature = SignatureModule.to.get<SignatureController>();
  });

  group('SignatureController Test', () {
    //   test("First Test", () {
    //     expect(signature, isInstanceOf<SignatureController>());
    //   });

    //   test("Set Value", () {
    //     expect(signature.value, equals(0));
    //     signature.increment();
    //     expect(signature.value, equals(1));
    //   });
  });
}
